
# Automatically added by dh_python2:
if which pycompile >/dev/null 2>&1; then
	pycompile -p gluu-flask 
fi

# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/gluu-flask" ] || [ -e "/etc/init/gluu-flask.conf" ]; then
	if [ ! -e "/etc/init/gluu-flask.conf" ]; then
		update-rc.d gluu-flask defaults >/dev/null
	fi
	invoke-rc.d gluu-flask start || exit $?
fi
# End automatically added section
