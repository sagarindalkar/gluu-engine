# Automatically added by dh_installinit
if [ -x "/etc/init.d/gluu-flask" ] || [ -e "/etc/init/gluu-flask.conf" ]; then
	invoke-rc.d gluu-flask stop || exit $?
fi
# End automatically added section

# Automatically added by dh_python2:
if which pyclean >/dev/null 2>&1; then
	pyclean -p gluu-flask 
else
	dpkg -L gluu-flask | grep \.py$ | while read file
	do
		rm -f "${file}"[co] >/dev/null
  	done
fi

# End automatically added section
