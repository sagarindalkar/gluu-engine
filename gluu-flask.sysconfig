# put this file at /etc/sysconfig/gluu-flask
# API environment set as production mode
API_ENV=prod

# IP address where salt-master listens to. By default uses IP address from eth0.
SALT_MASTER_IPADDR=`hostname -I | cut -d ' ' -f 1`

# Address where app is bind
HOST=127.0.0.1

# Port to listen to
PORT=8080
